[project]
name = "agentlab-core"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
  "jsonschema>=4.21"
]

[project.scripts]
lab = "agentlab_cli.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
